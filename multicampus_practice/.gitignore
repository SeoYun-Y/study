# 모든 하위 폴더에 있는 .env 파일을 무시함
**/.env

# 파이썬 캐시 폴더도 같이 무시하면 깔끔해요
**/__pycache__/

# 가상환경 venv 파일 제외
venv/

# 모델 체크포인트 (용량 큰 파일, GitHub 100MB 제한)
**/model2/
**/checkpoint-*/
*.safetensors
*.pt
*.bin
*.pth